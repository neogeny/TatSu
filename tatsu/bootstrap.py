#!/usr/bin/env python3
#
# CAVEAT UTILITOR
#
#  This file was automatically generated by 竜TatSu v5.18.0b1
#
#     https://pypi.python.org/pypi/tatsu/
#
#  Any changes you make to it will be overwritten the next time
#  the file is generated.
#
# ruff: noqa: RUF100, RUF102, C405, COM812, I001, F401, F811, PLR1702
# ruff: noqa: PLC2801, SIM117, PL2401, PLC2402, PLC2403
# fmt: off

from __future__ import annotations

from tatsu import decorators as tatsu
from tatsu.contexts import Ctx
from tatsu.parserconfig import ParserConfig
from tatsu.parsing import Parser, generic_main
from tatsu.tokenizing.buffer import Buffer
from tatsu.tokenizing.textlines import TextLinesTokenizer

__all__ = [
    'TatSuBootstrapTokenizer',
    'TatSuBootstrapBuffer',
    'TatSuBootstrapParser',
    'TatSuBootstrapRules',
    'main',
    ]


KEYWORDS: set[str] = set()


class TatSuBootstrapTokenizer(TextLinesTokenizer):
    def __init__(
        self,
        text, /,
        config: ParserConfig | None = None,
        **settings,
    ) -> None:
        config = ParserConfig.new(
            config=config,
            whitespace=r'(?m)\s+',
            nameguard=None,
            ignorecase=False,
            namechars='',
            parseinfo=True,
            comments=r'(?ms)[(][*]\s*((?:.|\n)*?)\s*[*][)]|[\/][*]\s*((?:.|\n)*?)\s*[*][\/]',
            eol_comments=r'(?ms)(?:[#]|[\/][\/])(.*?)$',
            keywords=KEYWORDS,
            start='start',
        )
        assert isinstance(config, ParserConfig)
        config = config.override(**settings)

        super().__init__(text, config=config)


class TatSuBootstrapBuffer(Buffer):  # NOTE: backwards compatibility
    def __init__(
        self,
        text, /,
        config: ParserConfig | None = None,
        **settings,
    ) -> None:
        config = ParserConfig.new(
            config=config,
            whitespace=r'(?m)\s+',
            nameguard=None,
            ignorecase=False,
            namechars='',
            parseinfo=True,
            comments=r'(?ms)[(][*]\s*((?:.|\n)*?)\s*[*][)]|[\/][*]\s*((?:.|\n)*?)\s*[*][\/]',
            eol_comments=r'(?ms)(?:[#]|[\/][\/])(.*?)$',
            keywords=KEYWORDS,
            start='start',
        )
        assert isinstance(config, ParserConfig)
        config = config.override(**settings)

        super().__init__(text, config=config)


class TatSuBootstrapParser(Parser):
    def __init__(self, /, config: ParserConfig | None = None, **settings):
        config = ParserConfig.new(config, **settings)
        rulessource = TatSuBootstrapRules()
        assert isinstance(config, ParserConfig)
        tokenizercls = config.tokenizercls or TatSuBootstrapTokenizer

        super().__init__(rulessource, config=config, tokenizercls=tokenizercls)


class TatSuBootstrapRules:
    def __init__(self, /, config: ParserConfig | None = None, **settings):
        config = ParserConfig.new(
            config=config,
            whitespace=r'(?m)\s+',
            nameguard=None,
            ignorecase=False,
            namechars='',
            parseinfo=True,
            comments=r'(?ms)[(][*]\s*((?:.|\n)*?)\s*[*][)]|[\/][*]\s*((?:.|\n)*?)\s*[*][\/]',
            eol_comments=r'(?ms)(?:[#]|[\/][\/])(.*?)$',
            keywords=KEYWORDS,
            start='start',
        )
        assert isinstance(config, ParserConfig)
        config = config.override(**settings)
        self._config = config

    @property
    def config(self) -> ParserConfig:
        assert isinstance(self._config, ParserConfig)
        return self._config

    @tatsu.rule
    def start(self, ctx: Ctx):
        self.grammar(ctx)

    @tatsu.rule('Grammar')
    def grammar(self, ctx: Ctx):
        ctx.define(['title'], ['directives', 'keywords', 'rules'])
        with ctx.nameset('title'):
            ctx.constant('TATSU')
        with ctx.loopopt() as cl:
            @cl.exp
            def _():
                with ctx.choice() as (ctxα, chα):
                    @chα.option
                    def _():
                        with ctxα.nameadd('directives'):
                            self.directive(ctxα)

                    @chα.option
                    def _():
                        with ctxα.nameadd('keywords'):
                            self.keyword(ctxα)

                    chα.expecting('<directive>', '<keyword>')
        with ctx.nameadd('rules'):
            self.rule(ctx)
        with ctx.loopopt() as cl:
            @cl.exp
            def _():
                with ctx.choice() as (ctxα, chα):
                    @chα.option
                    def _():
                        with ctxα.nameadd('rules'):
                            self.rule(ctxα)

                    @chα.option
                    def _():
                        with ctxα.nameadd('keywords'):
                            self.keyword(ctxα)

                    chα.expecting('<keyword>', '<rule>')
        ctx.eofcheck()

    @tatsu.rule
    def directive(self, ctx: Ctx):
        ctx.define(['name', 'value'], [])
        ctx.token('@@')
        with ctx.ifnot_():
            ctx.token('keyword')
        ctx.cut()
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    ctxα.define(['name', 'value'], [])
                    with ctxα.nameset('name'):
                        with ctxα.group():
                            with ctxα.choice() as (ctxβ, chβ):
                                @chβ.option
                                def _():
                                    ctxβ.token('comments')

                                @chβ.option
                                def _():
                                    ctxβ.token('eol_comments')

                                chβ.expecting('comments', 'eol_comments')
                    ctxα.cut()
                    ctxα.token('::')
                    ctxα.cut()
                    with ctxα.nameset('value'):
                        self.regex(ctxα)

                @chα.option
                def _():
                    ctxα.define(['name', 'value'], [])
                    with ctxα.nameset('name'):
                        with ctxα.group():
                            ctxα.token('whitespace')
                    ctxα.cut()
                    ctxα.token('::')
                    ctxα.cut()
                    with ctxα.nameset('value'):
                        with ctxα.group():
                            with ctxα.choice() as (ctxβ, chβ):
                                @chβ.option
                                def _():
                                    self.regex(ctxβ)

                                @chβ.option
                                def _():
                                    self.string(ctxβ)

                                @chβ.option
                                def _():
                                    ctxβ.token('None')

                                @chβ.option
                                def _():
                                    ctxβ.token('False')

                                @chβ.option
                                def _():
                                    ctxβ.constant('None')

                                chβ.expecting(
                                    '<regex>',
                                    '<string>',
                                    'False',
                                    'None'
                                )

                @chα.option
                def _():
                    ctxα.define(['name', 'value'], [])
                    with ctxα.nameset('name'):
                        with ctxα.group():
                            with ctxα.choice() as (ctxβ, chβ):
                                @chβ.option
                                def _():
                                    ctxβ.token('nameguard')

                                @chβ.option
                                def _():
                                    ctxβ.token('ignorecase')

                                @chβ.option
                                def _():
                                    ctxβ.token('left_recursion')

                                @chβ.option
                                def _():
                                    ctxβ.token('parseinfo')

                                @chβ.option
                                def _():
                                    ctxβ.token('memoization')

                                chβ.expecting(
                                    'ignorecase',
                                    'left_recursion',
                                    'memoization',
                                    'nameguard',
                                    'parseinfo'
                                )
                    ctxα.cut()
                    with ctxα.group():
                        with ctxα.choice() as (ctxβ, chβ):
                            @chβ.option
                            def _():
                                ctxβ.define(['value'], [])
                                ctxβ.token('::')
                                ctxβ.cut()
                                with ctxβ.nameset('value'):
                                    self.boolean(ctxβ)

                            @chβ.option
                            def _():
                                with ctxβ.nameset('value'):
                                    ctxβ.constant(True)

                            chβ.expecting('::')

                @chα.option
                def _():
                    ctxα.define(['name', 'value'], [])
                    with ctxα.nameset('name'):
                        with ctxα.group():
                            ctxα.token('grammar')
                    ctxα.cut()
                    ctxα.token('::')
                    ctxα.cut()
                    with ctxα.nameset('value'):
                        self.word(ctxα)

                @chα.option
                def _():
                    ctxα.define(['name', 'value'], [])
                    with ctxα.nameset('name'):
                        with ctxα.group():
                            ctxα.token('namechars')
                    ctxα.cut()
                    ctxα.token('::')
                    ctxα.cut()
                    with ctxα.nameset('value'):
                        self.string(ctxα)

                chα.expecting(
                    'comments',
                    'eol_comments',
                    'grammar',
                    'ignorecase',
                    'left_recursion',
                    'memoization',
                    'namechars',
                    'nameguard',
                    'parseinfo',
                    'whitespace'
                )
        ctx.cut()

    @tatsu.rule
    def keywords(self, ctx: Ctx):
        with ctx.loopplus() as cl:
            @cl.exp
            def _():
                self.keyword(ctx)

    @tatsu.rule
    def keyword(self, ctx: Ctx):
        ctx.token('@@keyword')
        ctx.cut()
        ctx.token('::')
        ctx.cut()
        with ctx.loopopt() as cl:
            @cl.exp
            def _():
                with ctx.nameadd('@'):
                    with ctx.group():
                        with ctx.choice() as (ctxα, chα):
                            @chα.option
                            def _():
                                self.word(ctxα)

                            @chα.option
                            def _():
                                self.string(ctxα)

                            chα.expecting('<string>', '<word>')
                with ctx.ifnot_():
                    with ctx.group():
                        with ctx.choice() as (ctxα, chα):
                            @chα.option
                            def _():
                                ctxα.token(':')

                            @chα.option
                            def _():
                                ctxα.token('=')

                            chα.expecting(':', '=')

    @tatsu.rule
    def params(self, ctx: Ctx):
        with ctx.nameadd('@'):
            self.first_param(ctx)
        with ctx.loopopt() as cl:
            @cl.exp
            def _():
                ctx.token(',')
                with ctx.nameadd('@'):
                    self.literal(ctx)
                with ctx.ifnot_():
                    ctx.token('=')
                ctx.cut()

    @tatsu.rule
    def first_param(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.path(ctxα)

            @chα.option
            def _():
                self.literal(ctxα)

            chα.expecting(
                '(?!\\d)\\w+(?:::(?!\\d)\\w+)+',
                '<boolean>',
                '<float>',
                '<hex>',
                '<int>',
                '<literal>',
                '<null>',
                '<path>',
                '<raw_string>',
                '<string>',
                '<word>'
            )

    @tatsu.rule
    def kwparams(self, ctx: Ctx):
        with ctx.gatherplus() as g:
            @g.sep
            def _():
                ctx.token(',')

            @g.exp
            def _():
                self.pair(ctx)

    @tatsu.rule
    def the_params_at_last(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                with ctxα.nameset('kwparams'):
                    self.kwparams(ctxα)

            @chα.option
            def _():
                ctxα.define(['kwparams', 'params'], [])
                with ctxα.nameset('params'):
                    self.params(ctxα)
                ctxα.token(',')
                ctxα.cut()
                with ctxα.nameset('kwparams'):
                    self.kwparams(ctxα)

            @chα.option
            def _():
                with ctxα.nameset('params'):
                    self.params(ctxα)

            chα.expecting('<kwparams>', '<params>')

    @tatsu.rule
    def paramdef(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                ctxα.define(['kwparams', 'params'], [])
                ctxα.token('[')
                ctxα.cut()
                with ctxα.choice() as (ctxβ, chβ):
                    @chβ.option
                    def _():
                        with ctxβ.nameset('kwparams'):
                            self.kwparams(ctxβ)

                    @chβ.option
                    def _():
                        ctxβ.define(['kwparams', 'params'], [])
                        with ctxβ.nameset('params'):
                            self.params(ctxβ)
                        ctxβ.token(',')
                        ctxβ.cut()
                        with ctxβ.nameset('kwparams'):
                            self.kwparams(ctxβ)

                    @chβ.option
                    def _():
                        with ctxβ.nameset('params'):
                            self.params(ctxβ)

                    chβ.expecting('<kwparams>', '<params>')
                ctxα.token(']')

            @chα.option
            def _():
                ctxα.define(['kwparams', 'params'], [])
                ctxα.token('(')
                ctxα.cut()
                with ctxα.choice() as (ctxβ, chβ):
                    @chβ.option
                    def _():
                        with ctxβ.nameset('kwparams'):
                            self.kwparams(ctxβ)

                    @chβ.option
                    def _():
                        ctxβ.define(['kwparams', 'params'], [])
                        with ctxβ.nameset('params'):
                            self.params(ctxβ)
                        ctxβ.token(',')
                        ctxβ.cut()
                        with ctxβ.nameset('kwparams'):
                            self.kwparams(ctxβ)

                    @chβ.option
                    def _():
                        with ctxβ.nameset('params'):
                            self.params(ctxβ)

                    chβ.expecting('<kwparams>', '<params>')
                ctxα.token(')')

            @chα.option
            def _():
                ctxα.define(['params'], [])
                ctxα.token('::')
                ctxα.cut()
                with ctxα.nameset('params'):
                    self.params(ctxα)

            chα.expecting('(', '::', '[')

    @tatsu.rule('Rule')
    def rule(self, ctx: Ctx):
        ctx.define(['base', 'decorators', 'exp', 'kwparams', 'name', 'params'], [])
        with ctx.nameset('decorators'):
            with ctx.loopopt() as cl:
                @cl.exp
                def _():
                    self.decorator(ctx)
        with ctx.nameset('name'):
            self.name(ctx)
        ctx.cut()
        with ctx.optional():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    ctxα.define(['kwparams', 'params'], [])
                    ctxα.token('[')
                    ctxα.cut()
                    with ctxα.choice() as (ctxβ, chβ):
                        @chβ.option
                        def _():
                            with ctxβ.nameset('kwparams'):
                                self.kwparams(ctxβ)

                        @chβ.option
                        def _():
                            ctxβ.define(['kwparams', 'params'], [])
                            with ctxβ.nameset('params'):
                                self.params(ctxβ)
                            ctxβ.token(',')
                            ctxβ.cut()
                            with ctxβ.nameset('kwparams'):
                                self.kwparams(ctxβ)

                        @chβ.option
                        def _():
                            with ctxβ.nameset('params'):
                                self.params(ctxβ)

                        chβ.expecting('<kwparams>', '<params>')
                    ctxα.token(']')

                @chα.option
                def _():
                    ctxα.define(['kwparams', 'params'], [])
                    ctxα.token('(')
                    ctxα.cut()
                    with ctxα.choice() as (ctxβ, chβ):
                        @chβ.option
                        def _():
                            with ctxβ.nameset('kwparams'):
                                self.kwparams(ctxβ)

                        @chβ.option
                        def _():
                            ctxβ.define(['kwparams', 'params'], [])
                            with ctxβ.nameset('params'):
                                self.params(ctxβ)
                            ctxβ.token(',')
                            ctxβ.cut()
                            with ctxβ.nameset('kwparams'):
                                self.kwparams(ctxβ)

                        @chβ.option
                        def _():
                            with ctxβ.nameset('params'):
                                self.params(ctxβ)

                        chβ.expecting('<kwparams>', '<params>')
                    ctxα.token(')')

                @chα.option
                def _():
                    ctxα.define(['params'], [])
                    ctxα.token('::')
                    ctxα.cut()
                    with ctxα.nameset('params'):
                        self.params(ctxα)

                chα.expecting('(', '::', '[')
        with ctx.optional():
            ctx.define(['base'], [])
            ctx.token('<')
            ctx.cut()
            with ctx.nameset('base'):
                self.known_name(ctx)
        ctx.void()
        ctx.pattern(r'=|:=?')
        ctx.cut()
        with ctx.nameset('exp'):
            self.expre(ctx)
        self.ENDRULE(ctx)
        ctx.cut()

    @tatsu.rule
    def ENDRULE(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                with ctxα.if_():
                    self.UNINDENTED(ctxα)

            @chα.option
            def _():
                self.EMPTYLINE(ctxα)
                with ctxα.optional():
                    ctxα.token(';')

            @chα.option
            def _():
                ctxα.eofcheck()

            @chα.option
            def _():
                ctxα.token(';')

            chα.expecting(
                '(?:\\s*(?:\\r?\\n|\\r)){2,}',
                '(?=\\s*(?:\\r?\\n|\\r)[^\\s])',
                ';',
                '<EMPTYLINE>',
                '<UNINDENTED>'
            )

    @tatsu.rule
    def UNINDENTED(self, ctx: Ctx):
        ctx.pattern(r'(?=\s*(?:\r?\n|\r)[^\s])')

    @tatsu.rule
    def EMPTYLINE(self, ctx: Ctx):
        ctx.pattern(r'(?:\s*(?:\r?\n|\r)){2,}')

    @tatsu.rule
    def decorator(self, ctx: Ctx):
        ctx.token('@')
        with ctx.ifnot_():
            ctx.token('@')
        ctx.cut()
        with ctx.nameset('@'):
            with ctx.group():
                with ctx.choice() as (ctxα, chα):
                    @chα.option
                    def _():
                        ctxα.token('override')

                    @chα.option
                    def _():
                        ctxα.token('name')

                    @chα.option
                    def _():
                        ctxα.token('isname')

                    @chα.option
                    def _():
                        ctxα.token('nomemo')

                    chα.expecting('isname', 'name', 'nomemo', 'override')

    @tatsu.rule
    def pair(self, ctx: Ctx):
        with ctx.nameadd('@'):
            self.word(ctx)
        ctx.token('=')
        ctx.cut()
        with ctx.nameadd('@'):
            self.literal(ctx)

    @tatsu.rule
    def expre(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.choice(ctxα)

            @chα.option
            def _():
                self.sequence(ctxα)

            chα.expecting(
                ';',
                '<EMPTYLINE>',
                '<ENDRULE>',
                '<UNINDENTED>',
                '<choice>',
                '<element>',
                '<option>',
                '<sequence>',
                '|'
            )

    @tatsu.rule('Choice')
    def choice(self, ctx: Ctx):
        with ctx.optional():
            ctx.token('|')
            ctx.cut()
        with ctx.nameadd('@'):
            self.option(ctx)
        with ctx.loopplus() as cl:
            @cl.exp
            def _():
                ctx.token('|')
                ctx.cut()
                with ctx.nameadd('@'):
                    self.option(ctx)

    @tatsu.rule('Option')
    def option(self, ctx: Ctx):
        with ctx.nameset('@'):
            self.sequence(ctx)

    @tatsu.rule('Sequence')
    def sequence(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                with ctxα.if_():
                    with ctxα.group():
                        self.element(ctxα)
                        ctxα.token(',')
                with ctxα.gatherplus() as g:
                    @g.sep
                    def _():
                        ctxα.token(',')

                    @g.exp
                    def _():
                        self.element(ctxα)

            @chα.option
            def _():
                with ctxα.loopplus() as cl:
                    @cl.exp
                    def _():
                        with ctxα.ifnot_():
                            self.ENDRULE(ctxα)
                        self.element(ctxα)

            chα.expecting(
                '(?:\\s*(?:\\r?\\n|\\r)){2,}',
                '(?=\\s*(?:\\r?\\n|\\r)[^\\s])',
                ';',
                '<EMPTYLINE>',
                '<ENDRULE>',
                '<UNINDENTED>',
                '<element>',
                '<named>',
                '<override>',
                '<rule_include>',
                '<term>'
            )

    @tatsu.rule
    def element(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.rule_include(ctxα)

            @chα.option
            def _():
                self.named(ctxα)

            @chα.option
            def _():
                self.override(ctxα)

            @chα.option
            def _():
                self.term(ctxα)

            chα.expecting(
                '<atom>',
                '<closure>',
                '<cut>',
                '<cut_deprecated>',
                '<empty_closure>',
                '<gather>',
                '<join>',
                '<left_join>',
                '<lookahead>',
                '<named>',
                '<named_list>',
                '<named_single>',
                '<negative_lookahead>',
                '<optional>',
                '<override>',
                '<override_list>',
                '<override_single>',
                '<override_single_deprecated>',
                '<positive_closure>',
                '<right_join>',
                '<rule_include>',
                '<skip_to>',
                '<term>',
                '<void>',
                '>'
            )

    @tatsu.rule('RuleInclude')
    def rule_include(self, ctx: Ctx):
        ctx.token('>')
        ctx.cut()
        with ctx.nameset('@'):
            self.known_name(ctx)

    @tatsu.rule
    def named(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.named_list(ctxα)

            @chα.option
            def _():
                self.named_single(ctxα)

            chα.expecting('<name>', '<named_list>', '<named_single>')

    @tatsu.rule('NamedList')
    def named_list(self, ctx: Ctx):
        ctx.define(['exp', 'name'], [])
        with ctx.nameset('name'):
            self.name(ctx)
        ctx.token('+:')
        ctx.cut()
        with ctx.nameset('exp'):
            self.term(ctx)

    @tatsu.rule('Named')
    def named_single(self, ctx: Ctx):
        ctx.define(['exp', 'name'], [])
        with ctx.nameset('name'):
            self.name(ctx)
        ctx.token(':')
        ctx.cut()
        with ctx.nameset('exp'):
            self.term(ctx)

    @tatsu.rule
    def override(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.override_list(ctxα)

            @chα.option
            def _():
                self.override_single(ctxα)

            @chα.option
            def _():
                self.override_single_deprecated(ctxα)

            chα.expecting(
                '<override_list>',
                '<override_single>',
                '<override_single_deprecated>',
                '@',
                '@+:',
                '@:'
            )

    @tatsu.rule('OverrideList')
    def override_list(self, ctx: Ctx):
        ctx.token('@+:')
        ctx.cut()
        with ctx.nameset('@'):
            self.term(ctx)

    @tatsu.rule('Override')
    def override_single(self, ctx: Ctx):
        ctx.token('@:')
        ctx.cut()
        with ctx.nameset('@'):
            self.term(ctx)

    @tatsu.rule('Override')
    def override_single_deprecated(self, ctx: Ctx):
        ctx.token('@')
        ctx.cut()
        with ctx.nameset('@'):
            self.term(ctx)

    @tatsu.rule
    def term(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.void(ctxα)

            @chα.option
            def _():
                self.gather(ctxα)

            @chα.option
            def _():
                self.join(ctxα)

            @chα.option
            def _():
                self.left_join(ctxα)

            @chα.option
            def _():
                self.right_join(ctxα)

            @chα.option
            def _():
                self.empty_closure(ctxα)

            @chα.option
            def _():
                self.positive_closure(ctxα)

            @chα.option
            def _():
                self.closure(ctxα)

            @chα.option
            def _():
                self.optional(ctxα)

            @chα.option
            def _():
                self.skip_to(ctxα)

            @chα.option
            def _():
                self.lookahead(ctxα)

            @chα.option
            def _():
                self.negative_lookahead(ctxα)

            @chα.option
            def _():
                self.cut(ctxα)

            @chα.option
            def _():
                self.cut_deprecated(ctxα)

            @chα.option
            def _():
                self.atom(ctxα)

            chα.expecting(
                '!',
                '&',
                '(',
                '()',
                '->',
                '<alert>',
                '<atom>',
                '<call>',
                '<closure>',
                '<constant>',
                '<cut>',
                '<cut_deprecated>',
                '<dot>',
                '<empty_closure>',
                '<eof>',
                '<gather>',
                '<group>',
                '<join>',
                '<left_join>',
                '<lookahead>',
                '<negative_lookahead>',
                '<optional>',
                '<pattern>',
                '<positive_closure>',
                '<right_join>',
                '<skip_to>',
                '<token>',
                '<void>',
                '>>',
                '[',
                '{',
                '{}',
                '~'
            )

    @tatsu.rule('Group')
    def group(self, ctx: Ctx):
        ctx.token('(')
        ctx.cut()
        with ctx.nameset('@'):
            self.expre(ctx)
        ctx.token(')')
        ctx.cut()

    @tatsu.rule
    def gather(self, ctx: Ctx):
        with ctx.if_():
            with ctx.group():
                self.atom(ctx)
                ctx.token('.{')
        ctx.cut()
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    self.positive_gather(ctxα)

                @chα.option
                def _():
                    self.normal_gather(ctxα)

                chα.expecting('<normal_gather>', '<positive_gather>')

    @tatsu.rule('PositiveGather')
    def positive_gather(self, ctx: Ctx):
        ctx.define(['exp', 'sep'], [])
        with ctx.nameset('sep'):
            self.atom(ctx)
        ctx.token('.{')
        with ctx.nameset('exp'):
            self.expre(ctx)
        ctx.token('}')
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    ctxα.token('+')

                @chα.option
                def _():
                    ctxα.token('-')

                chα.expecting('+', '-')
        ctx.cut()

    @tatsu.rule('Gather')
    def normal_gather(self, ctx: Ctx):
        ctx.define(['exp', 'sep'], [])
        with ctx.nameset('sep'):
            self.atom(ctx)
        ctx.token('.{')
        ctx.cut()
        with ctx.nameset('exp'):
            self.expre(ctx)
        ctx.token('}')
        with ctx.optional():
            ctx.token('*')
            ctx.cut()
        ctx.cut()

    @tatsu.rule
    def join(self, ctx: Ctx):
        with ctx.if_():
            with ctx.group():
                self.atom(ctx)
                ctx.token('%{')
        ctx.cut()
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    self.positive_join(ctxα)

                @chα.option
                def _():
                    self.normal_join(ctxα)

                chα.expecting('<normal_join>', '<positive_join>')

    @tatsu.rule('PositiveJoin')
    def positive_join(self, ctx: Ctx):
        ctx.define(['exp', 'sep'], [])
        with ctx.nameset('sep'):
            self.atom(ctx)
        ctx.token('%{')
        with ctx.nameset('exp'):
            self.expre(ctx)
        ctx.token('}')
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    ctxα.token('+')

                @chα.option
                def _():
                    ctxα.token('-')

                chα.expecting('+', '-')
        ctx.cut()

    @tatsu.rule('Join')
    def normal_join(self, ctx: Ctx):
        ctx.define(['exp', 'sep'], [])
        with ctx.nameset('sep'):
            self.atom(ctx)
        ctx.token('%{')
        ctx.cut()
        with ctx.nameset('exp'):
            self.expre(ctx)
        ctx.token('}')
        with ctx.optional():
            ctx.token('*')
            ctx.cut()
        ctx.cut()

    @tatsu.rule('LeftJoin')
    def left_join(self, ctx: Ctx):
        ctx.define(['exp', 'sep'], [])
        with ctx.nameset('sep'):
            self.atom(ctx)
        ctx.token('<{')
        ctx.cut()
        with ctx.nameset('exp'):
            self.expre(ctx)
        ctx.token('}')
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    ctxα.token('+')

                @chα.option
                def _():
                    ctxα.token('-')

                chα.expecting('+', '-')
        ctx.cut()

    @tatsu.rule('RightJoin')
    def right_join(self, ctx: Ctx):
        ctx.define(['exp', 'sep'], [])
        with ctx.nameset('sep'):
            self.atom(ctx)
        ctx.token('>{')
        ctx.cut()
        with ctx.nameset('exp'):
            self.expre(ctx)
        ctx.token('}')
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    ctxα.token('+')

                @chα.option
                def _():
                    ctxα.token('-')

                chα.expecting('+', '-')
        ctx.cut()

    @tatsu.rule('PositiveClosure')
    def positive_closure(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                ctxα.token('{')
                with ctxα.nameset('@'):
                    self.expre(ctxα)
                ctxα.token('}')
                with ctxα.group():
                    with ctxα.choice() as (ctxβ, chβ):
                        @chβ.option
                        def _():
                            ctxβ.token('-')

                        @chβ.option
                        def _():
                            ctxβ.token('+')

                        chβ.expecting('+', '-')
                ctxα.cut()

            @chα.option
            def _():
                with ctxα.nameset('@'):
                    self.atom(ctxα)
                ctxα.token('+')
                ctxα.cut()

            chα.expecting(
                '(',
                '<alert>',
                '<atom>',
                '<call>',
                '<constant>',
                '<dot>',
                '<eof>',
                '<group>',
                '<pattern>',
                '<token>',
                '{'
            )

    @tatsu.rule('Closure')
    def closure(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                ctxα.token('{')
                with ctxα.nameset('@'):
                    self.expre(ctxα)
                ctxα.token('}')
                with ctxα.optional():
                    ctxα.token('*')
                ctxα.cut()

            @chα.option
            def _():
                with ctxα.nameset('@'):
                    self.atom(ctxα)
                ctxα.token('*')
                ctxα.cut()

            chα.expecting(
                '(',
                '<alert>',
                '<atom>',
                '<call>',
                '<constant>',
                '<dot>',
                '<eof>',
                '<group>',
                '<pattern>',
                '<token>',
                '{'
            )

    @tatsu.rule('EmptyClosure')
    def empty_closure(self, ctx: Ctx):
        ctx.token('{}')
        ctx.cut()
        with ctx.nameset('@'):
            ctx.void()

    @tatsu.rule('Optional')
    def optional(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                ctxα.token('[')
                ctxα.cut()
                with ctxα.nameset('@'):
                    self.expre(ctxα)
                ctxα.token(']')
                ctxα.cut()

            @chα.option
            def _():
                with ctxα.nameset('@'):
                    self.atom(ctxα)
                with ctxα.ifnot_():
                    with ctxα.group():
                        with ctxα.choice() as (ctxβ, chβ):
                            @chβ.option
                            def _():
                                ctxβ.token('?"')

                            @chβ.option
                            def _():
                                ctxβ.token("?'")

                            @chβ.option
                            def _():
                                ctxβ.token('?/')

                            chβ.expecting("?'", '?"', '?/')
                ctxα.token('?')
                ctxα.cut()

            chα.expecting(
                '(',
                '<alert>',
                '<atom>',
                '<call>',
                '<constant>',
                '<dot>',
                '<eof>',
                '<group>',
                '<pattern>',
                '<token>',
                '['
            )

    @tatsu.rule('Lookahead')
    def lookahead(self, ctx: Ctx):
        ctx.token('&')
        ctx.cut()
        with ctx.nameset('@'):
            self.term(ctx)

    @tatsu.rule('NegativeLookahead')
    def negative_lookahead(self, ctx: Ctx):
        ctx.token('!')
        ctx.cut()
        with ctx.nameset('@'):
            self.term(ctx)

    @tatsu.rule('SkipTo')
    def skip_to(self, ctx: Ctx):
        ctx.token('->')
        ctx.cut()
        with ctx.nameset('@'):
            self.term(ctx)

    @tatsu.rule
    def atom(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.group(ctxα)

            @chα.option
            def _():
                self.token(ctxα)

            @chα.option
            def _():
                self.alert(ctxα)

            @chα.option
            def _():
                self.constant(ctxα)

            @chα.option
            def _():
                self.call(ctxα)

            @chα.option
            def _():
                self.pattern(ctxα)

            @chα.option
            def _():
                self.dot(ctxα)

            @chα.option
            def _():
                self.eof(ctxα)

            chα.expecting(
                '$',
                '(',
                '/./',
                '<alert>',
                '<call>',
                '<constant>',
                '<dot>',
                '<eof>',
                '<group>',
                '<pattern>',
                '<raw_string>',
                '<regex>',
                '<string>',
                '<token>',
                '<word>',
                '\\^+',
                '`'
            )

    @tatsu.rule('Call')
    def call(self, ctx: Ctx):
        self.word(ctx)

    @tatsu.rule('Void')
    def void(self, ctx: Ctx):
        ctx.token('()')
        ctx.cut()

    @tatsu.rule('Fail')
    def fail(self, ctx: Ctx):
        ctx.token('!()')
        ctx.cut()

    @tatsu.rule('Cut')
    def cut(self, ctx: Ctx):
        ctx.token('~')
        ctx.cut()

    @tatsu.rule('Cut')
    def cut_deprecated(self, ctx: Ctx):
        ctx.token('>>')
        ctx.cut()

    @tatsu.rule
    def known_name(self, ctx: Ctx):
        self.name(ctx)
        ctx.cut()

    @tatsu.rule
    def name(self, ctx: Ctx):
        self.word(ctx)

    @tatsu.rule('Constant')
    def constant(self, ctx: Ctx):
        with ctx.if_():
            ctx.token('`')
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    ctxα.pattern(r'(?ms)```((?:.|\n)*?)```')

                @chα.option
                def _():
                    ctxα.token('`')
                    with ctxα.nameset('@'):
                        self.literal(ctxα)
                    ctxα.token('`')

                @chα.option
                def _():
                    ctxα.pattern(r'`(.*?)`')

                chα.expecting('(?ms)```((?:.|\\n)*?)```', '`', '`(.*?)`')

    @tatsu.rule('Alert')
    def alert(self, ctx: Ctx):
        ctx.define(['level', 'message'], [])
        with ctx.nameset('level'):
            ctx.pattern(r'\^+')
        with ctx.nameset('message'):
            self.constant(ctx)

    @tatsu.rule('Token')
    def token(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.string(ctxα)

            @chα.option
            def _():
                self.raw_string(ctxα)

            chα.expecting(
                '<doublequoted>',
                '<multiline_string>',
                '<raw_string>',
                '<singlequoted>',
                '<string>',
                'r'
            )

    @tatsu.rule
    def literal(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.string(ctxα)

            @chα.option
            def _():
                self.raw_string(ctxα)

            @chα.option
            def _():
                self.boolean(ctxα)

            @chα.option
            def _():
                self.word(ctxα)

            @chα.option
            def _():
                self.hex(ctxα)

            @chα.option
            def _():
                self.float(ctxα)

            @chα.option
            def _():
                self.int(ctxα)

            @chα.option
            def _():
                self.null(ctxα)

            chα.expecting(
                '(?!\\d)\\w+',
                '0[xX](?:\\d|[a-fA-F])+',
                '<boolean>',
                '<doublequoted>',
                '<float>',
                '<hex>',
                '<int>',
                '<multiline_string>',
                '<null>',
                '<raw_string>',
                '<singlequoted>',
                '<string>',
                '<word>',
                'False',
                'None',
                'True',
                '[-+]?(?:\\d+\\.\\d*|\\d*\\.\\d+)(?:[Ee][-+]?\\d+)?',
                '[-+]?\\d+',
                'r'
            )

    @tatsu.rule
    def string(self, ctx: Ctx):
        with ctx.group():
            with ctx.choice() as (ctxα, chα):
                @chα.option
                def _():
                    self.multiline_string(ctxα)

                @chα.option
                def _():
                    self.singlequoted(ctxα)

                @chα.option
                def _():
                    self.doublequoted(ctxα)

                chα.expecting(
                    "(?ms)'''((?:\\\\\\\\|\\\\.|(?!''').)*?)'''",
                    '(?ms)"""((?:\\\\\\\\|\\\\.|(?!""").)*?)"""',
                    '<DOUBLEQUOTED>',
                    '<SINGLEQUOTED>',
                    '<doublequoted>',
                    '<multiline_string>',
                    '<singlequoted>'
                )

    @tatsu.rule
    def singlequoted(self, ctx: Ctx):
        self.SINGLEQUOTED(ctx)

    @tatsu.rule
    def doublequoted(self, ctx: Ctx):
        self.DOUBLEQUOTED(ctx)

    @tatsu.rule
    def raw_string(self, ctx: Ctx):
        ctx.pattern(r'r')
        with ctx.nameset('@'):
            self.STRING(ctx)

    @tatsu.rule
    def STRING(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.SINGLEQUOTED(ctxα)

            @chα.option
            def _():
                self.DOUBLEQUOTED(ctxα)

            chα.expecting(
                "'((?:[^'\\n]|\\\\'|\\\\\\\\)*?)'",
                '"((?:[^"\\n]|\\\\"|\\\\\\\\)*?)"',
                '<DOUBLEQUOTED>',
                '<SINGLEQUOTED>'
            )

    @tatsu.rule
    def SINGLEQUOTED(self, ctx: Ctx):
        ctx.pattern(r"'((?:[^'\n]|\\'|\\\\)*?)'")
        ctx.cut()

    @tatsu.rule
    def DOUBLEQUOTED(self, ctx: Ctx):
        ctx.pattern(r'"((?:[^"\n]|\\"|\\\\)*?)"')
        ctx.cut()

    @tatsu.rule
    def multiline_string(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                ctxα.pattern(r"(?ms)'''((?:\\\\|\\.|(?!''').)*?)'''")
                ctxα.cut()

            @chα.option
            def _():
                ctxα.pattern(r'(?ms)"""((?:\\\\|\\.|(?!""").)*?)"""')
                ctxα.cut()

            chα.expecting(
                "(?ms)'''((?:\\\\\\\\|\\\\.|(?!''').)*?)'''",
                '(?ms)"""((?:\\\\\\\\|\\\\.|(?!""").)*?)"""'
            )

    @tatsu.rule
    def hex(self, ctx: Ctx):
        ctx.pattern(r'0[xX](?:\d|[a-fA-F])+')

    @tatsu.rule
    def float(self, ctx: Ctx):
        ctx.pattern(r'[-+]?(?:\d+\.\d*|\d*\.\d+)(?:[Ee][-+]?\d+)?')

    @tatsu.rule
    def int(self, ctx: Ctx):
        ctx.pattern(r'[-+]?\d+')

    @tatsu.rule
    def path(self, ctx: Ctx):
        ctx.pattern(r'(?!\d)\w+(?:::(?!\d)\w+)+')

    @tatsu.rule
    def word(self, ctx: Ctx):
        ctx.pattern(r'(?!\d)\w+')

    @tatsu.rule('Dot')
    def dot(self, ctx: Ctx):
        ctx.token('/./')

    @tatsu.rule('Pattern')
    def pattern(self, ctx: Ctx):
        self.regex(ctx)

    @tatsu.rule
    def regex(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                self.REGEX(ctxα)

            @chα.option
            def _():
                ctxα.token('?')
                with ctxα.nameset('@'):
                    self.STRING(ctxα)

            @chα.option
            def _():
                self.deprecated_regex(ctxα)

            chα.expecting(
                '(?ms)/((?:[^/\\\\]|\\\\/|\\\\.)*)/',
                '<REGEX>',
                '<deprecated_regex>',
                '?',
                '?/'
            )

    @tatsu.rule
    def REGEX(self, ctx: Ctx):
        ctx.pattern(r'(?ms)/((?:[^/\\]|\\/|\\.)*)/')
        ctx.cut()

    @tatsu.rule
    def deprecated_regex(self, ctx: Ctx):
        ctx.token('?/')
        ctx.cut()
        with ctx.nameset('@'):
            ctx.pattern(r'(?:.|\n)*?(?=/\?)')
        ctx.pattern(r'/\?+')
        ctx.cut()
        ctx.alert('deprecated', 2)
        with ctx.ifnot_():
            ctx.void()
        ctx.cut()

    @tatsu.rule
    def boolean(self, ctx: Ctx):
        with ctx.choice() as (ctxα, chα):
            @chα.option
            def _():
                ctxα.token('True')

            @chα.option
            def _():
                ctxα.token('False')

            chα.expecting('False', 'True')

    @tatsu.rule
    def null(self, ctx: Ctx):
        ctx.token('None')

    @tatsu.rule('EOF')
    def eof(self, ctx: Ctx):
        ctx.token('$')
        ctx.cut()


def main(filename, **kwargs):
    if not filename or filename == '-':
        import sys

        text = sys.stdin.read()
    else:
        import pathlib

        text = pathlib.Path(filename).read_text()

    parser = TatSuBootstrapParser()
    return parser.parse(text, filename=filename, **kwargs)


if __name__ == '__main__':
    import json
    from tatsu.util import asjson

    ast = generic_main(main, TatSuBootstrapParser, name='TatSuBootstrap')
    data = asjson(ast)
    print(json.dumps(data, indent=2))
