machine:
    python:
        version: 3.5.3

dependencies:
    override:
        - pip install tox tox-pyenv
        - pyenv local 2.7.13 3.4.3 3.5.3 3.6.1 pypy-5.7.1

test:
    override:
        - tox
